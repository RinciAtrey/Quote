plugins {
    id "com.android.application"
    id "kotlin-android"
    id "dev.flutter.flutter-gradle-plugin"
}


android {
    namespace = "com.example.quotes_daily"
    compileSdk = 34 // Replace flutter.compileSdkVersion if dynamic
    ndkVersion = "25.2.9519653" // Adjust to match your NDK version

    compileOptions{
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
        coreLibraryDesugaringEnabled true

    }

    kotlinOptions {
        jvmTarget = "17"
    }

    defaultConfig {
        applicationId = "com.example.quotes_daily"
        minSdk = 21 // Replace flutter.minSdkVersion if dynamic
        targetSdk = 34 // Replace flutter.targetSdkVersion if dynamic
        versionCode = 1 // Replace flutter.versionCode if dynamic
        versionName = "1.0" // Replace flutter.versionName if dynamic
    }

    buildTypes {
        release {
            shrinkResources false
            signingConfig = signingConfigs.debug // Replace with a proper signing config for production
            minifyEnabled false // Disable ProGuard during development
        }
    }
}


flutter {
    source = "../.."
}
dependencies {
    coreLibraryDesugaring 'com.android.tools:desugar_jdk_libs:2.1.4'
}