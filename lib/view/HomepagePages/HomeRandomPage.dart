import 'package:flutter/material.dart';

class HomeRandomPage extends StatelessWidget {
  const HomeRandomPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: ElevatedButton(onPressed: (){},
          child: Text("Discover Quotes")),
    );
  }
}
